"use client";

import { useEffect, useState } from "react";
import { formatPrice } from "@/lib/beat-catalog";

type Order = { orderNumber: string; status: "pending" | "approved" | "rejected" | "cancelled" | "refunded"; totalCents: number; downloads: { url: string; expiresAt: string; remaining: number }[] };
export function PaymentStatus({ orderNumber }: { orderNumber?: string }) { const [order, setOrder] = useState<Order | null>(null); const [error, setError] = useState(""); useEffect(() => { if (!orderNumber) return; let active = true; const check = async () => { try { const response = await fetch(`/api/orders/${encodeURIComponent(orderNumber)}`, { cache: "no-store" }); const body = await response.json(); if (!response.ok) throw new Error(body.message ?? "Pedido não encontrado."); if (active) { setOrder(body); if (body.status === "pending") window.setTimeout(check, 10_000); } } catch (reason) { if (active) setError(reason instanceof Error ? reason.message : "Não foi possível consultar o pedido."); } }; check(); return () => { active = false; }; }, [orderNumber]); if (!orderNumber) return <p>Quando você voltar do Mercado Pago, o status seguro do pedido aparecerá aqui.</p>; if (error) return <p>{error}</p>; if (!order) return <p>Consultando a confirmação segura do seu pedido...</p>; if (order.status === "approved") return <div className="payment-status payment-status--approved"><p>PAGAMENTO APROVADO</p><strong>Pedido: #{order.orderNumber}</strong><span>Total: {formatPrice(order.totalCents)}</span><p>Os dados da compra foram enviados para seu e-mail.</p>{order.downloads.map((download, index) => <a key={download.url} href={download.url}>BAIXAR ARQUIVOS {index + 1} ↗</a>)}</div>; return <div className="payment-status"><p>{order.status === "rejected" ? "PAGAMENTO RECUSADO" : "PAGAMENTO EM ANÁLISE"}</p><strong>Pedido: #{order.orderNumber}</strong><span>O download só será liberado após a confirmação do pagamento.</span></div>; }
